### 项目中的难点 
订单一定是广告投放系统中最复杂的部分。我负责了这块业务的需求迭代和部分重构。
1. 引入前端唯一标识 资源位的选取随着产品的迭代，由最初的3级结构，转变成至少5级的不定级层级结构。引入前端唯一标识，解决了资源排重的问题
2. 可维护行升级
   最初的业务逻辑都放在了methods中，运用了大量的if，switch判断，和promise回调处理状态变更，导致数据更新无法追踪。
   将排期状态由数字 转化为语义化常量
   用getters 维护判断条件，而取代原来的if语句
   用action 将主要的业务函数放在action中，而非维护在业务代码。
   用mutation 维护几个重要的操作状态
   推动后端将原有的数组结构转换为对象(key-value), 前端根据参数的优先级进行排序，三元运算符。解决了数组的删除查找每次都需要遍历带来的执行效率低的问题。
   运用reduce，every,some，filter等方法，以及引入numeral，大大减少了订单计算部分的代码量
   运用对象来维护多状态，比如
   {
       enable,
       active,
       locked,
       disabled,
       error,
       is_half
   }

创意的组件化升级
1. 